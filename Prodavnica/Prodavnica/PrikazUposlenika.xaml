<Page
    x:Class="Prodavnica.PrikazUposlenika"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Prodavnica"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ListView x:Name="prikazUposlenika" HorizontalAlignment="Left" Height="457" Margin="0,10,0,0" VerticalAlignment="Top" Width="414">
            <ListView.ItemTemplate>
                <DataTemplate>                    
                    <StackPanel Orientation="Horizontal">
                        <TextBlock xml:space="preserve"  Text="Ime: " />
                        <TextBlock xml:space="preserve" Text="{Binding Ime}" FontWeight="Bold" />
                        <TextBlock xml:space="preserve" Text=" " FontWeight="Bold" />
                        <TextBlock xml:space="preserve" Text="{Binding Prezime}" FontWeight="Bold" />
                        
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</Page>
